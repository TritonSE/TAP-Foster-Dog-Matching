/**
 *
 * Application Context
 *
 * Used in Application page and ApplicationProgress component
 * (see pages/Application.js for more details)
 */

import React from "react";

const ApplicationContext = React.createContext({});

export default ApplicationContext;
